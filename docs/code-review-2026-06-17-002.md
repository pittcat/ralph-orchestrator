# Code Review Report

**Scope**: 8 commits, 28 files, +3246 / -13 lines (base `722c46d` → HEAD `1b4b75b`)
**Branch**: `ralph/2026-06-17-002-feat-ce-executor-step-handoff` (worktree)
**Mode**: Interactive
**Plan**: `docs/plans/2026-06-17-002-feat-ce-executor-step-handoff-plan.md` (plan_source: explicit)

## Intent

建立 Step Handoff Mechanism（SHM）：多步 plan 在阶段边界的静态可证 + 运行时 SLA + 磁盘状态一致 + payload 硬门。8 单元（U1–U8）已全部 commit。

## Review Team

| Reviewer | Reason |
|---|---|
| correctness (always) | — |
| testing (always) | — |
| maintainability (always) | — |
| project-standards (always) | — |
| ce-agent-native-reviewer (always) | — |
| ce-learnings-researcher (always) | — |
| ce-adversarial-reviewer (cross-cutting) | diff > 50 lines + preset 拓扑变更 + handoff 状态机 |
| ce-reliability-reviewer (cross-cutting) | HandoffTracker / SLA / escalation / preset fail-closed |
| ce-security-reviewer (cross-cutting) | preset config 校验路径 + 多消费者白名单 |

---

## Findings

### P1 — High

| # | File | Issue | Reviewer(s) | Conf | Route |
|---|------|-------|-------------|------|-------|
| 1 | `crates/ralph-core/src/event_loop/mod.rs:887-893` | `apply_step_handoff_gate` 注入 `plan.blocked` 时 `source = event.hat.unwrap_or('plan-gate')`：当 executor 等非 plan-gate hat 触发 gate 时，EventOriginGuard 会因源 hat 不在 `publishes` 列表而拒收 `plan.blocked`；仅 `plan-gate` 是当前 preset 唯一合法 publisher，semantically 不通用 | correctness, security, adversarial, reliability | 100 | gated_auto → downstream-resolver |
| 2 | `crates/ralph-core/src/event_loop/mod.rs:899` | 新加的 diagnostic topic `event.step_handoff.gate_rejected` 不在 `ORCHESTRATOR_DIAGNOSTIC_TOPICS` 白名单；会在 `process_parse_result` 重读时被 isolated per-turn budget 误丢，observability 受损 | correctness | 75 | safe_auto → review-fixer |
| 3 | `presets/en/ce-executor-isolated.yml` + `crates/ralph-core/src/config/hat.rs:514-521` + `ralph_config.rs:438-468` | `trigger_multi_consumer_topics` 字段未约束为已知 topic 集合；typo（如 `fix.exhaused`）不会被 `validate_ambiguous_routing` 捕获；preset 可注入任意字符串绕过 1:1 校验 | security, adversarial | 100 | gated_auto → downstream-resolver |
| 4 | `crates/ralph-core/src/step_handoff/progress_task_gate.rs` + `event_loop/mod.rs:846-915` | progress_task_gate 拒绝路径**不写** `recovery.jsonl` envelope，与 HandoffTracker escalation 不一致；`ralph diagnose` 看不到 `progress_task_mismatch` finding；plan L475 要求 `runtime-diagnosis.md` 补排查段，未执行 | reliability, ce-agent-native-reviewer, adversarial | 75 | safe_auto → review-fixer |
| 5 | `crates/ralph-core/src/step_handoff/progress_task_gate.rs:286-308` | fail-closed 默认 + cold start（无 progress.md）首步立即 `plan.blocked` → 死循环风险；需 cold-start 豁免或初始化语义 | reliability, ce-learnings-researcher | 75 | gated_auto → downstream-resolver |

### P2 — Moderate

| # | File | Issue | Reviewer(s) | Conf | Route |
|---|------|-------|-------------|------|-------|
| 6 | `crates/ralph-core/src/event_loop/mod.rs:923-942` | `extract_step_and_task_id` 对非 JSON payload 静默返回 `(None, None)`，gate 退化为惰性通过——agent 写错 payload 格式不会被拦 | correctness, adversarial, security | 100 | gated_auto → downstream-resolver |
| 7 | `crates/ralph-core/src/step_handoff/progress_task_gate.rs:195` | `ProgressSnapshot::parse` 用 `split_once(':')` 截断 step 名；带冒号的 step 名（如 `step-02: validate-foo`）会被截断 | correctness | 75 | safe_auto → review-fixer |
| 8 | `crates/ralph-core/src/event_loop/mod.rs:6821` | `apply_step_handoff_gate` 位置在 state machine 之后、workflow guards 之前；gate 拒绝的 `plan.blocked` 注入会绕过 workflow guard terminal 闭包检查，且无法 undo state machine instance 接受状态 | correctness, adversarial | 75 | gated_auto → downstream-resolver |
| 9 | `crates/ralph-core/src/step_handoff/progress_task_gate.rs:325` | `plan.complete` 检查分支：若 payload 无 step 且无 task_id，gate 完全跳过验证返回 `Aligned` | correctness | 75 | gated_auto → downstream-resolver |
| 10 | `crates/ralph-core/src/event_loop/mod.rs:858` | gate 拒绝时删原 event + publish plan.blocked，违反 per-turn sticky business-event budget；plan-gate 同 turn 内无法 emit queue.advance 修复 | correctness, adversarial | 75 | manual → downstream-resolver |
| 11 | `crates/ralph-core/src/config/ralph_config.rs:450` | `validate_ambiguous_routing` 在所有 hat 都声明白名单时通过，但缺"至少两个 hat 实际消费该 topic"的校验——空操作白名单可通过 | correctness | 75 | gated_auto → downstream-resolver |
| 12 | `crates/ralph-cli/tests/policy_check_handoff.rs:60-152` | 测试硬编码 required_fields，未 cross-check SSOT YAML；SSOT 改字段名后 test 仍绿，acceptance 失效 | security, adversarial, ce-agent-native-reviewer | 75 | gated_auto → downstream-resolver |
| 13 | `crates/ralph-core/src/event_policy.rs` + 多处 | NULL_PAYLOAD_REJECT_TOPICS 扩展 3 个 topic 后，schema-less 路径可能绕过；缺 Observe 模式与 Strict 模式一致性测试 | correctness, adversarial | 75 | manual → downstream-resolver |
| 14 | `crates/ralph-core/src/event_loop/mod.rs` + `workflow_contract.rs:113-122` | 30s 硬编码 SLA 在 multi-step plan 累计不合理（5 步 × 30s = 150s）；cold start 慢 backend 触发频繁 escalation | adversarial, reliability | 75 | manual → downstream-resolver |
| 15 | `presets/en/ce-executor-isolated.yml:1587-1646` | `fix.exhausted`/`debug.exhausted` 多消费者（plan-gate + shipper）→ plan.blocked + REVIEW_COMPLETE 交叉路径，缺回归覆盖 | reliability | 75 | manual → downstream-resolver |
| 16 | `crates/ralph-core/src/event_policy.rs:419-538` | null payload 拒发时 finding.message 仅 "WAC R10"，缺具体缺失字段提示；agent 修复路径不可观察 | reliability | 75 | manual → downstream-resolver |
| 17 | `crates/ralph-core/src/step_handoff/progress_task_gate.rs:312-322` | `tasks.jsonl` 损坏 JSON line 时 fail-closed 还是 skip-on-error 行为未明确，无 fixture | reliability | 75 | manual → downstream-resolver |
| 18 | `docs/guide/runtime-diagnosis.md` | SLA 监控不可见：`ralph diagnose` 不统计 `handoff_dispatch_timeout` / `progress_task_mismatch` 频次 | reliability, ce-agent-native-reviewer | 75 | gated_auto → downstream-resolver |
| 19 | `crates/ralph-core/data/ralph-tools-emit.md` + `.claude/skills/ralph-tools/SKILL.md` | 6 个新加 handoff topic + `trigger_multi_consumer_topics` / `progress_task_gate` 概念在 ralph-tools 文档**完全没有提到**；agent 收到 `task.resume` payload 不知道怎么修复 | ce-agent-native-reviewer | 75 | gated_auto → downstream-resolver |
| 20 | `crates/ralph-cli/src/hats.rs:756-808` | `ralph hats show <hat>` 不输出 `trigger_multi_consumer_topics`；agent 无法发现 multi-consumer 路由 | ce-agent-native-reviewer | 75 | gated_auto → downstream-resolver |
| 21 | `crates/ralph-cli` precheck | `ralph emit --policy-check` 不接 U4 step_handoff gate；agent 在 CLI 试 emit `queue.advance` 不会被预检拦 | ce-agent-native-reviewer | 75 | gated_auto → downstream-resolver |

### P3 — Low (selected; 完整 P3 见下 demoted buckets)

| # | File | Issue | Reviewer(s) | Conf | Route |
|---|------|-------|-------------|------|-------|
| 22 | `docs/plans/2026-06-17-002-feat-ce-executor-step-handoff-plan.md:39,295` | Plan 行号引用 `event_policy.rs:412` 已漂移到 `:495`；L295 描述 `NULL_PAYLOAD_REJECT_TOPICS` 含 6 topic，实际已扩到 9；违反 CLAUDE.md "反向验证（必须）" | project-standards, adversarial, security | 100 | safe_auto → review-fixer |
| 23 | `scripts/ralph-zsh-plugin.zsh:67` | 补全描述缺 "adversarial"，与 `presets/index.json` 不一致（pre-existing，本 diff 范围未触发新变更） | project-standards | 75 | advisory |
| 24 | `crates/ralph-core/src/event_loop/mod.rs:868-869` | `event.hat` fallback 用裸字符串字面量 `"plan-gate"` 而非 `HatId` 类型 | maintainability | 75 | safe_auto → review-fixer |
| 25 | `crates/ralph-cli/tests/policy_check_handoff.rs:61-159` | `schema_for` / `full_required_fields` 三处 SSOT 字段表重复 | maintainability | 75 | advisory |
| 26 | `crates/ralph-core/src/event_loop/tests/termination.rs:557-761` | U6 verdict gate 测试 ~22 行 setup 重复 4 次 | maintainability, testing | 75 | advisory |
| 27 | `presets/en/ce-executor-isolated.yml` (now ~2075 lines) | 单文件跨 2k 行；plan-gate instructions 加 ~50 行 inline UTF-8 | maintainability | 75 | advisory |
| 28 | `crates/ralph-core/src/config/hat.rs:521` | `trigger_multi_consumer_topics` 是 `Vec<String>` 但语义是 Set；YAML order-sensitivity | maintainability | 50 | advisory |

---

## Demoted to soft buckets (P2/P3 advisory-only)

下列 finding 因 severity P2/P3 + autofix_class=advisory + 仅 testing/maintainability 触发，按 Stage 5 step 6c 规则移入软桶。`ralph diagnose` / 后续 review 可读取，但不在主 findings 表：

- **Residual risks** (testing, 7 项):
  - `policy_check_handoff.rs` Rust 端硬编码 SSOT 而非读 preset YAML
  - HandoffTracker U2 测缺 boundary 30s 显式断言
  - `test_progress_task_mismatch` 未显式反向断言 queue.advance 被 reject
  - `test_e2e_step_handoff_topology_complete` 不跑 config.validate()
  - plan-gate instructions 改 plan_name 等字段无 golden prompt 测
  - progress.md typo graceful-fail 未显式测
  - whitelist hat + 非 whitelist hat 混合负向边界未测

- **Maintainability** (13 项):
  - progress_task_gate.rs 669 行可拆 progress_parser.rs
  - `apply_step_handoff_gate` / `extract_step_and_task_id` 可抽 `gate/` 子模块
  - `validate_ambiguous_routing` 双 pass 可改单 pass
  - `extract_step_and_task_id` 与既有 extractor 重复
  - scenario runner setup ~120 行重复
  - U8 三 scenario fixture 形状相似（"如果第 3 个 multi-consumer scenario 出现再抽 helper"）
  - progress_parser `strip_bold_label` 字符串分配
  - `step_handoff` 与 `workflow_contract` 模块命名重叠
  - ...

- **Maintainability residual risks** (5 项):
  - apply_step_handoff_gate publishes plan.blocked from any source hat (EventOriginGuard boundary)
  - ProgressSnapshot::parse 三种格式与 plan-gate instructions 仅锁一种漂移
  - `u6_verdict_gate_fake_pass_on_report_done_after_upstream_fail` 测试名易被误读
  - StepHandoffConfig 在 preset level 不可见，无 CLI 显示
  - policy_check_handoff chain 2 subprocess cost 0.4-2s

- **Maintainability testing gaps** (6 项):
  - ProgressSnapshot::parse 无 proptest
  - trigger_multi_consumer_topics 缺 3+ hat 测试
  - HandoffTracker config-driven timeout boundary 未测
  - plan.blocked 非 plan-gate source 路径未测
  - NULL_PAYLOAD_REJECT_TOPICS 9 项 rationale 未文档化
  - policy_check_handoff 未覆盖 publish allowlist 一致性

---

## Pre-existing

- `presets/en/ce-executor-isolated.yml:84-93, 1487-1491, 1584-1593` 描述"3 次 Hard → plan.blocked Final"分档在 plan L413 表中描述，但 U2 commit 已显式采用选项 A（single Hard escalation），plan 内部不一致（pre-existing）
- `scripts/ralph-zsh-plugin.zsh:67` 描述缺 "adversarial"（pre-existing，与本 diff 范围无关）

---

## Requirements Completeness (plan_source: explicit)

Plan: `docs/plans/2026-06-17-002-feat-ce-executor-step-handoff-plan.md`

| Unit | Requirements | Status |
|------|--------------|--------|
| U1 (R-A1–R-A5, R-E1, SC2) | plan-gate triggers 闭包 + WAC strict 零 error | ✅ Met (commit 8bc309a) |
| U2 (R-B1, R-B2, R-B4, SC1) | work.ready 30s activation + Hard escalation | ✅ Met (commit 2df5f77, 选项 A 单次 escalation) |
| U3 (R-B3, R-F4) | dual-publish 回归对 + 第三 business event 拒 | ✅ Met (commit 90953a0) |
| U4 (R-C1, R-F3, SC1) | Progress-Task 硬门 | ✅ Met (commit 248912e) |
| U5 (R-C2, R-D1, R-D2, SC3) | synth terminal + handoff payload 硬门 | ✅ Met (commit 81c4799) |
| U6 (R-C3) | verdict gate 闭包验证 | ✅ Met (commit 93950f3) |
| U7 (R-D3) | SSOT 四消费链验收 | ✅ Met (commit ae533b6) |
| U8 (R-F1–R-F4, SC1–SC5, R-E2) | multi-step E2E + 全量回归 | ✅ Met (commit 1b4b75b) |

**Plan 文件本身需要小修**：
- L295 描述 NULL_PAYLOAD_REJECT_TOPICS 6→9 未同步（finding #22）
- L39 event_policy.rs:412 → :495 行号漂移（同上）
- L413 表 "3 次 Hard → plan.blocked Final" 与 U2 实际 single escalation 不一致（finding #23）

> 实施单元全部完成。Plan 文件本身 3 处小漂移。

---

## Applied Fixes

无（本轮仅做 review，未运行 fix 阶段）。

---

## Residual Actionable Work

下游需要 follow-up 的 finding（不进入 safe_auto 自动修复，按优先级排序）：

### 必须修复（P1 修复 → 决策前阻塞）

| 优先级 | Finding | 决策 |
|---|---|---|
| HIGH | #1 source hat 选择（4 reviewer 命中） | 需架构决策：source=plan-gate 硬编码 vs with_target 路由 vs 改走 recovery |
| HIGH | #3 whitelist topic 枚举约束（2 reviewer 命中） | 需 schema enum 化或增加 topic 校验 |
| HIGH | #4 recovery envelope 一致性（3 reviewer 命中） | 需在 gate reject 路径调 record_recovery_envelope |
| HIGH | #5 cold-start 豁免（2 reviewer 命中） | 需首次步允许空 progress.md |

### 应修复（P2 / 中期）

| 优先级 | Finding |
|---|---|
| MEDIUM | #6 / #7 / #9 / #11 / #18 / #19 / #20 / #21 |
| LOW | #8 / #10 / #13 / #14 / #15 / #16 / #17 |

---

## Learnings & Past Solutions

`docs/solutions/integration-issues/ce-executor-isolated-preset-dispatch-gap-plan-gate-executor-2026-06-12.md` 是本 SHM 的直接前身。SHM 8 单元完整闭合了该 solution 描述的根因 1/2/3/4。建议 U8 后给该 solution 加 frontmatter `superseded_by: 2026-06-17-002`。

`docs/solutions/developer-experience/wac-rollout-tiered-gates-2026-06-12.md` KTD-9 `multi_consumer_topics` 窄语义被 U1 继承（plan L71 已预警 `consumer` 变 None）——确认合规使用。

`docs/solutions/integration-issues/ce-executor-wave-emission-must-batch-in-single-emit-2026-06-09.md` 强调 "HARD RULE + contract test + 失败模式引用" 模式。U1 plan-gate instructions 仅补 3-5 行，无 HARD RULE 标记，建议补强。

---

## Agent-Native Gaps

1. `ralph hats show <hat>` 不输出 `trigger_multi_consumer_topics`（finding #20）
2. `ralph-tools-emit.md` / `ralph-tools.md` 完全没提 6 个新 handoff topic + 2 个新概念（finding #19）
3. step_handoff gate 注入的 `plan.blocked` payload 是自由文本（`PROGRESS_TASK_GATE_REJECTED: ...`），非 JSON object —— agent 无法用 `jq` 解析
4. gate reject 不写 recovery envelope（finding #4 包含）—— `ralph diagnose` 看不到
5. `ralph emit --policy-check` 不接 U4 step_handoff gate（finding #21）—— agent 缺 early feedback
6. agent 站在 fixer 视角看不到 `fix.exhausted` 触发 plan-gate 二次消费

---

## Coverage

| Bucket | Count |
|---|---|
| Total raw findings (across 9 reviewers) | ~80 |
| Suppressed by confidence gate (< 75) | ~12 |
| Mode-aware demotion (P2/P3 advisory + testing/maintainability) | 26 |
| Pre-existing separated | 2 |
| **Primary findings (P0+P1+P2+P3 routed)** | **28** |
| safe_auto → review-fixer queue | 4 (#2, #7, #22, #24) |
| gated_auto → downstream-resolver | 11 |
| manual → downstream-resolver | 7 |
| advisory → human/release | 6 |

**Failed/timed-out reviewers**: 0
**Untracked files excluded**: 无
**Intent uncertainty**: 无（plan 文档完整，commit messages 一致）

---

## Verdict

**Ready with fixes**（需在合并前修 4 个 P1 项 + 1 个 plan 文件漂移；P2 11 项可入 follow-up）

**修复顺序**：

1. **safe_auto**（自动修）：#22 plan 行号同步 + #2 diagnostic 白名单 + #7 parser 冒号截断 + #24 HatId 类型化 → 1 个 commit
2. **gated_auto**（需要决策）：#1 source hat 策略 → 优先 source=plan-gate 硬编码；#3 whitelist topic 枚举 → 接受现状（preset 作者负责），加 unit test；#4 recovery envelope → 直接接 `record_recovery_envelope`；#5 cold-start → 加 first-step 豁免
3. **manual**（入 plan 后续单元或独立 plan）

预计 1 个 fix commit（safe_auto）+ 1 个 gated_auto commit（source hat + recovery envelope）= 10 个 commit 收尾。

---

Report complete.
