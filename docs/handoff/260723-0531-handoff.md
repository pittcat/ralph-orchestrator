# U7/U8 supervisor 下游路由闭合 — 交接（已闭合）

## 结论
U7 主阻塞已解；U8 对账见 `docs/solutions/architecture-patterns/2026-07-23-002-u8-closure-reconciliation.md`。
全量基线由维护者本地跑。

## 根因（已修）
Isolated 下 `build_prompt("ralph")` 误 drain 全员 pending，偷走 `plan.complete` → shipper/reporter 永不激活。

## 本轮关键修复
- isolated ralph 只消费自己的 pending + multi-consumer stall skip
- wave phase Collect→Done；R13 `finalize_terminal_cleanup`
- fault：空事件 + success=false → failure；timeout-with-events 仍可见
- Pi 合同测对齐 `--no-skills --skill .agents/skills`（保留你的 Pi 行为）
- U6：non-blocking(1 fix) / blocking(2 fix) / nonce markers / review shared-readonly

## 测试
```bash
cargo nextest run -p ralph-cli --test integration_supervisor_primary
cargo nextest run -p ralph-cli --bin ralph -- test_execute_wave_hat_backend_ test_execute_wave_keeps_
```
