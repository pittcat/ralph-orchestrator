# DAG Scheduler 执行面接线 — Agent 交接文档(2026-09-08 22:45,第二棒)

接替 `docs/handoff/260908-1016-handoff.md`(第一棒)。E1、E2、E3 运行时接线已完成并提交；当前只剩 Phase C/D 的证据与文档收口。

# 当前目标

不变:完成 plan `docs/plans/2026-09-03-0959-feat-parallel-forge-runtime-dag-scheduler-plan.md` 的执行面接线,范围 = `presets/en/parallel-forge-preset-author-notes.md` 顶部 7 条 promote 前置义务全部;终验 `./scripts/run-tests.sh` 全绿。

# 本会话新增提交(分支 `pittcat-dev`,全部验证绿)

| commit | 内容 |
|---|---|
| `ec22a6ba` | E1/E2 store 接缝补完:接手时工作区有外部会话留下的半成品(unclosed delimiter 编译失败 + `IntegrationStore` 两方法缺 rusqlite 实现),主代理修复并提交。内容:durable job journal(`dag_store_rusqlite/jobs.rs`,`reserve_job`/`record_job_pid`/`accept_job_terminal`/`unresolved_jobs`,token fencing)、v15 `dag_integration_intents` + `prepare_intent/get_intent`(memory+rusqlite)、`worktree/resume.rs` `UnitWorktree::resume`(fail-closed 只读恢复)、`PtySpawnSpec.clear_env`、preflight.rs rustfmt 修复 |
| `5e8a02e5` | **E1 执行接线**:新建 `dag_scheduler/spawn.rs`(1395 行)。dag 模式 tick 真实 spawn:admission→journal `reserve_job` 围栏→hat 模板 prompt→verified-base `UnitWorktree::acquire`→pty_kernel spawn→`record_job_pid`→tokio lease loop;完成经 completion channel→`drain_completions`→`merge_one`(每 tick 至多 1 条,FileEventMergeSink 写主账本)→journal terminal(post-acceptance)。driver 路由:executed→reviewer、reviewed ACCEPTED→verifier、REJECTED→fixer(bump_attempt,3 次预算,耗尽合成 execution_failed)。`DagPools` fixer 池用 in-flight 计数执行(D16;pipeline `Stage` 无 Fix 变体,fix 复用 Review slot)。`parallel_forge_handoff.rs` 加 `ExecutionPlanUnit.tests`(additive,digest 不受影响)。inner.rs +34→4921 行 |
| `03f5b9eb` | **E2 integration + 收口**:新建 `dag_scheduler/integrate.rs`(814 行)。verified→`queue_integration` 去重入队→每 tick 按 `integration_order` 串行 integrate 1 个;unit_commit 从 git refs 读(不信 agent 自报);空 tests gate fail-closed;StaleExpected/TargetBusy 重排;其余失败合成 `forge.unit.execution_failed`。`integration.rs` 在 gate Pass 后、CAS 前插 `prepare_intent`。FF 成功 emit `forge.unit.integrated`(IO 失败 requeue);accepted 后(=close_task 投影已跑)写 durable ack + `plan.integrated`;全部 ack 后过 v16 fence `try_record_terminal_emit`(INSERT OR IGNORE,PK `(plan_key,topic)`)恰好一次 emit `forge.exec.development.done`(append_supervisor_coord_event 形态)。admission `integrated_units` 从 durable store 水合解锁后继。schema `state_projection` 加 `forge.unit.integrated` close_task(D25);presets.rs inventory 测试同步。inner.rs 拆 `inner_prologue.rs` 后 4861 行 |
| `825f262a` | **E3 store 接缝**(E3 子代理被中止前完成的部分,主代理验证提交):v17 `dag_plan_meta`(approval-time verified_base_commit,INSERT OR IGNORE 钉一次,冲突 fail-closed)、`has_terminal_emit`(fence 探测)、`jobs.rs::list_jobs`(全量 journal 历史供水合) |
| `21c7567e` | DAG admission 使用 verified artifact 的 resource capacities/claims；补充资源合同落到 handoff 与运行时超售防护测试 |
| `d5d8a00b` | `scheduler_mode: dag` 下关闭 legacy SupervisorBridge，DAG runtime 成为唯一执行权威；统一四池容量解析并补 preflight/runtime 测试 |
| `9374323b` | 真实 Git 临时仓库 + 隔离 worktree + PTY 子进程 + `forge.unit.executed` 的 executor canary |
| `75b3cc44` | PTY hard-cap 超时改为杀整个 Unix child process group，并补 descendant 回归测试 |
| `7c34e7c4` | mock E2E cassette/scenario 改验 active DAG unit receipts（executed/reviewed/integrated） |
| `a66c1ed4` | integration gate 命令解析支持引号与反斜杠转义，仍以 argv 方式执行，不启用 shell |
| `944e1035` | 任一 gate 命令存在未闭合引号/转义即 fail-closed；防止 `filter_map` 静默丢弃检查，并补回归测试 |
| `c619fdab` | 真实 DAG recovery 覆盖 reserve→PID handshake 前、死 PID、integration event/ack 两种顺序；断言阻断、typed failure 与安全重发 |
| `d0459727` | 覆盖 terminal fence 已落盘但 `development.done` 事件缺失的恢复窗口，禁止重复不可逆 emit |
| `73553972` | accepted unit/correction/integrated 事件增加 plan-topology ownership fence，拒绝跨计划与未知 Unit 伪造，并补回归测试 |
| `f2f8b1ff` | accepted 回读按 `forge:<plan>:<unit>` 计划限定键清理 pending terminal，修复成功 job terminal 可能无法落 journal 的缺陷 |
| `ec46efbe` | author notes 对齐上述完成项与剩余 evidence gaps |
| `a680fc9b` | 恢复证据补齐：真实存活 PID 接管不重启、accepted 终态经计划限定回读后写入 durable journal |
| `fe837b9a` | R19/S17 身份围栏：accepted worker 结果由 runtime 注入并校验 `job_id/job_token/stage/attempt` 与 source hat，拒绝 stale/forged 结果推进流水线 |
| `052b8de2` | Phase D 定向文档审计：修正 author/review scheduler-mode references 对 DAG runtime ownership 与 side-effect boundary 的过时表述 |
| `21926817` | 补 durable-journal recovery 分支的伪造 accepted 结果回归测试：重启后无 pending map 时仍校验 journal 当前 job 的完整 identity |
| `9a06c5c6` | 真实延迟 PTY worker 重启 canary：第一 runtime 启动计数为 1，第二 runtime 从 durable journal 接管结果，重启后启动计数仍为 1 |
| `b1d5e869` | 真实 Git integration + SQLite ack + completion fence 的重启计数测试：第二 runtime 直接走 `recover_after_restart`，`forge.unit.integrated` 与 `forge.exec.development.done` 均保持单条 |
| `d395ba7f` | 将重启计数升级为两个真实 Git sibling branch：U1/U2 均真实 integration，重启后 integrated 事件保持 2 条、development.done 保持 1 条 |
| `f9188810` | 记录多 Unit 重启证据与最新 targeted 结果 |
| `aa34812d` | 新增真实 EventLoop BDD：accepted `forge.unit.integrated` 通过 state projection 关闭匹配 task，并断言 durable task ledger 状态 |
| `036b85bd` | 单任务 close projection 对已关闭终态幂等：重复 `forge.unit.integrated` 不改写关闭时间；真实 EventLoop BDD 扩展为两个 producer 重放同一 integrated payload |
| `2d727b7c` | 新增真实 DAG correction EventLoop BDD：review 拒绝进入单次 correction，fresh attempt 重新执行并通过验证，禁止落入 `work.failed` |
| `63c4116c` | 新增真实 DAG resume EventLoop BDD：从 accepted executed 边界直接恢复到 reviewer，不重复 executor 激活 |

验证记录(每次提交前主代理独立复跑):E1: dag_scheduler 126/126、TG-S05 绿、clippy 零告警;E2: dag_scheduler 132/132、core dag 124/124、presets 73/73、TG-S05 绿、clippy 零告警、ralph-cli 全量 2082 passed、ralph-core 全量 5464 passed;E3 store: 59/59、clippy 零告警;本轮 gate parser/integration 8/8、parallel-forge scenarios 22/22;`a680fc9b` 后全量 Phase 1 8681/8681、Phase 2 143/143、doctest 19/19；`fe837b9a` 后全量 Phase 1 8682/8682、Phase 2 143/143、doctest 19/19；`036b85bd` 后 projector replay 1/1、integrated-task-close EventLoop BDD 1/1；`2d727b7c` 后 DAG correction BDD 1/1；`63c4116c` 后 DAG resume BDD 1/1；完整 gate 在 `2d727b7c` 前为 Phase 1 8688/8688、Phase 2 143/143、doctest 19/19，resume 场景新增后待重跑。

# 关键决策(E1/E2 新定型,不要重开)

1. **durable 决策日志** = launch journal + plan/receipt 行,不需要单独 per-tick decision API(mod.rs:150-159 注释已按此写)。
2. **D16 四池**:fixer cap 由 spawn seam 的 `fixer_in_flight` 计数执行。
3. **integration 失败不新增 topic**:合成 `forge.unit.execution_failed`(TG-S06 钉死 `forge.unit.*` 族 6 成员)。
4. **`forge.exec.development.done` 恰好一次**:v16 `dag_terminal_emits` PK fence,fence 赢才 emit;fence 赢后落盘失败宁可漏发不可重发(恢复诊断为 blocked,E3 窗口 5)。
5. **ack 语义**:ack 只在 `forge.unit.integrated` 被 acceptance 接受后写 ⇒ `development.done` 隐含所有 close_task 投影已 acknowledged。
6. **unit_commit 从 git refs 读**,不信 agent payload。
7. **inner.rs 接缝纪律**:4861/5000,E3 挂点按设计稿与 E1 attach 块一起搬进 `inner_prologue.rs` helper,净减行。

# E3 运行时接线（已完成）

**设计稿:`.ralph/review/2026-09-03-0959-feat-parallel-forge-runtime-dag-scheduler-plan/scratch/e3-design.md`(110 行,gitignore 保护,禁止 commit)。** 核心内容:

- 勘察结论:resume 时 `sync_event_reader_to_file_end()` 使历史 trusted-file 事件不重读 ⇒ recovery 必须从 durable DAG store 重建全部运行时状态;emit 路径 == trusted events 文件,无路径 bug;nix 0.29(signal feature)已是 ralph-cli 依赖,pid 活性用 `kill(Pid, None)`(Ok/EPERM=alive,ESRCH=dead,其他=unknown)。
- `recover_after_restart()` 挂 inner.rs redrive 块旁(resume && dag 门):list_active_plans→rebuild_topology→verified_base(缺→blocked)→水合 plan.integrated 与 pipeline(`restore_unit` 待新增)→已 verified 未 integrated → queue_integration→逐 unresolved job 决策(events 有结果=Resume-Settle 喂 completion channel;pid alive=Adopt+tokio reaper;pid dead=Resume-Failure 镜像 fail_job;pid None/unknown=Blocked 不猜)→integration 对账(unacked:ledger 无事件=Re-emit,有=Blocked)→development.done fence 对账(fence 有 ledger 无=Blocked)→failed-terminal 对账(ledger 无失败事件=重合成)。
- crash matrix 6 窗口定义完整(reserve 后 spawn 前→blocked;pid 未写→blocked;terminal 未写→resume;integrated 未 ack 两分支;fence 赢 event 未落盘→blocked)。
- 结构:`collect_recovery_report(workspace, loop_id, journal, main_events_file) -> RecoveryPlan`(I/O 采集+纯决策,可独立测试),runtime 方法只 apply。
- 进度勾选:v17/store 方法、recovery planner/collect/apply、`blocked_plans`、recovered completion、pipeline restore、inner_prologue 挂点、全量回归与 clippy 均已完成；完整 crash matrix 仍属于后续证据任务。
- 「Blocked(budget) 合成 fix identity 无真实 reservation」结论:自洽,无需改码(store fail-closed 拒绝仅 warn)。

# E3 之后剩余(不变,按优先级)

1. **BDD×6（已补 integrated task close、DAG correction 与 DAG resume）**: immediate refill、receipt crash、env/path guard、sibling candidate、final once 的计划级独立真实 EventLoop 场景仍未补齐；attempt forgery 已由 DAG runtime ingress 的真实身份围栏测试覆盖，但尚未补成计划要求的独立 EventLoop BDD 场景。现有 `ralph-cli` DAG integration/recovery 测试是真实 CLI scheduler seam，不应冒充 `run_workflow_guard_scenario` BDD。
2. **完整 authoritative DAG crash matrix**:已有真实 executor canary，并已覆盖 reserve→PID handshake 前、死 PID、live PID adoption、真实 worker 重启不重复 spawn、两个真实 Git sibling 的 integration 与 `development.done` 重开计数、integration event/ack 两分支、terminal persist/projection、terminal fence 缺事件，以及重启后 durable-journal identity fence；核心 exactly-once 证据已齐，但仍可继续增强更多 crash point 的多进程计数覆盖。
3. **Phase D 文档收口**:本轮已完成 `.cursor/rules/*.mdc`、author/review scheduler-mode references、`scripts/check-cli-doc-drift.sh`、preset review anchor pytest 与 `ralph ... presets` 结构化校验；后续若再改 CLI/schema，仍需按规则增量复核 `docs/guide/cli-reference.md` 与两套 preset references。
4. **终验**:本轮已执行 `./scripts/run-tests.sh` 全量并通过；若后续修改代码，需重新执行。

# 重要注意事项(沿用第一棒 + 本会话新增)

- 测试硬规则:nextest + env scrub 前缀(见第一棒);supervisor/migrations `--features supervisor-db`;终验 `./scripts/run-tests.sh`。
- **并发外部会话纪律不变**:提交统一主代理做;开子代理前 `git status --porcelain` + 查 `.ralph/post-merge/findings/`。本会话开工时外部会话留了编译失败的半成品(ec22a6ba 已修复提交)——接手时务必先 `cargo check`。
- **子代理经验**:agent-0 连续完成了 E1/E2,上下文保留宝贵,下一棒优先 `Agent(resume="agent-0", prompt="E3 按 scratch/e3-design.md 继续")`;403 配额中断后 resume 可恢复(本会话遇到一次,稍后 resume 即成功)。曾因用户手动停止浪费过一次完整 prompt——resume 用短 prompt 即可。
- 行数:inner.rs 4861/5000;单文件 ≤5000;最大文件 integration.rs 1755。
- CLAUDE.md ≡ AGENTS.md;人类输出中文、代码注释英文。
- E1/E2 遗留风险(子代理报告原文,供 E3/后续收口):integration 在 tick 内同步执行会阻塞主 loop;integration allowlist 交叉校验恒真;`DAG_ENV_ALLOWLIST` 是 25 名 curated 集(D23 完整 credential declaration 后续)。此前 `tests` 条目 `split_whitespace` 不支持引号的问题已由 `a66c1ed4` 修复，非法条目静默丢弃问题由 `944e1035` 修复。
- 已知全绿基线:HEAD `63c4116c`；本轮 DAG correction BDD `1/1`、DAG resume BDD `1/1` 通过；上一完整 gate 为 Phase 1 `8688/8688`、Phase 2 `143/143`、doctest `19/19`。本次新增 BDD 后尚未重跑完整 `./scripts/run-tests.sh`。
